export class Url {
    link: string = '';
    displayText: string = '';

    constructor(link: string, displayText: string) {
        this.link = link;
        this.displayText = displayText;
    }
}

